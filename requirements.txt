# Framework web + serveur
fastapi==0.136.0
uvicorn[standard]==0.44.0

# Scheduler in-process
apscheduler==3.11.2

# Database (Postgres)
sqlalchemy==2.0.49
psycopg2-binary==2.9.11

# Config
pydantic==2.13.2
pydantic-settings==2.13.1
python-dotenv==1.2.2

# LLM
anthropic==0.96.0

# HTTP + scraping
httpx==0.28.1
requests==2.33.1
beautifulsoup4==4.14.3
feedparser==6.0.12

# Templates
jinja2==3.1.6

# Robustesse : retries backoff exponentiel sur les appels externes
tenacity==9.1.4

# Observabilité : capture d'erreurs en prod (optionnel, activé via SENTRY_DSN)
sentry-sdk[fastapi,sqlalchemy]==2.58.0

# Logs structurés (JSON en prod, lisible en dev)
structlog==25.5.0

# Auth : JWT HS256 pour sessions magic-link
PyJWT==2.10.1
